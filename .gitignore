# editor / tooling cruft
.Rhistory

# loose source images — the published gallery lives in gallery/
# (originals are kept locally in _source-images/)
*.png
screenshot1_GCHI.webp
screenshot2_extent_GCHI.webp

# large build artifacts are NOT in this repo (they live in ~/cgl-depot and
# ship as a GitHub Release asset, not in the source tree)
*.pmtiles
*.osm.pbf
demand_data.json
buildings_index.json
*.gz
*.zip

# local working files (keep out of the published repo)
runbook_CHI.md
DEMAND_POINTS.csv
BUILD_TIMELINE.md
METADATA_UPDATE_DRAFT.md
